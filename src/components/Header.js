import React from 'react';
import logo from '../images/logo-allocine.svg';

const Header = () => (
  <header className="header">
    <div className="header-centered">
      <img src={logo} alt="allocine logo" />
    </div>
  </header>
);

export default Header;
